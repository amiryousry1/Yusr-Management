# Yusr Control Center V1.2

افتح `index.html` مباشرة. الـCSS والـJavaScript مدمجين داخل الملف حتى لا تظهر نسخة HTML خام عند فتحه منفردًا.

- مصدر البيانات: Google Sheet الخاص بـYusr.
- الوضع: Read-only.
- التعديل: من Google Sheet فقط.
- التحديث: زر `تحديث` أو تلقائي حسب Dashboard Config.
- عند تعذر القراءة المباشرة: يظهر Snapshot للمعاينة مع تنبيه واضح.

راجع `AUDIT.md` لفهم مشاكل النسخة السابقة وما تم إصلاحه وحدود النظام.
